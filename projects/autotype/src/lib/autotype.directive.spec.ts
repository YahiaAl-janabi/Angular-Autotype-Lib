import { AutotypeDirective } from './autotype.directive';

describe('AutotypeDirective', () => {
  it('should create an instance', () => {
    const directive = new AutotypeDirective();
    expect(directive).toBeTruthy();
  });
});
